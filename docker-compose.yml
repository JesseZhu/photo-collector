version: "3.8"

services:
  photo-collector:
    build: .
    container_name: photo-collector
    restart: unless-stopped
    ports:
      - "3000:3000"
    environment:
      - JWT_SECRET=Jesse&Ann..
    volumes:
      - /volume1/docker/photo-collector:/app/data
      - /volume1/docker/photo-collector/uploads:/app/uploads
      # - /Users/Jesse/Documents/docker/photo-collector:/app/data
      # - /Users/Jesse/Documents/docker/photo-collector/uploads:/app/uploads
